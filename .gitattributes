# MT: this whole file is mine; upstream has no .gitattributes.
# Normalize all text files to LF. The pre-1.0 fork committed CRLF, which made
# every upstream diff look like the whole file changed.
* text=auto eol=lf
